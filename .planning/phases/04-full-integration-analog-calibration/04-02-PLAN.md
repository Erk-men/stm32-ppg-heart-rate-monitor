---
phase: 04-full-integration-analog-calibration
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c
autonomous: false
requirements: [OUT-02, OUT-04]
must_haves:
  truths:
    - "algorithm.c defines FINGER_MIN_AMPLITUDE with a value derived from the Plan 1 hardware measurement"
    - "algorithm_process() tracks a rolling raw-sample amplitude window (~1.28s)"
    - "When amplitude < FINGER_MIN_AMPLITUDE the firmware prints '--\\r\\n' and resets the adaptive threshold and state"
    - "After finger removal, re-acquisition succeeds because s_threshold, s_state, and the rolling BPM average are reset"
    - "With DEBUG_VERBOSE defined, every sample prints a labeled RAW/FILT/THR/AMP/ST line"
  artifacts:
    - path: /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c
      provides: "OUT-02 no-finger detection + threshold reset, OUT-04 DEBUG_VERBOSE per-sample dump"
      contains: "#define FINGER_MIN_AMPLITUDE"
  key_links:
    - from: "algorithm_process() amplitude window"
      to: "no-finger reset path"
      via: "amplitude < FINGER_MIN_AMPLITUDE triggers --\\r\\n and state reset"
      pattern: "FINGER_MIN_AMPLITUDE"
    - from: "algorithm_process() per-sample"
      to: "USART2"
      via: "#ifdef DEBUG_VERBOSE RAW/FILT/THR/AMP/ST dump"
      pattern: "#ifdef DEBUG_VERBOSE"
---

<objective>
Add no-finger detection and a per-sample diagnostic dump to algorithm.c. No-finger
detection (OUT-02) tracks a rolling amplitude window of the raw signal; when the
amplitude drops below the empirically measured FINGER_MIN_AMPLITUDE, the firmware
prints "--\r\n" and resets the adaptive threshold and state machine so a clean
re-acquisition succeeds on the next finger placement. DEBUG_VERBOSE (OUT-04) prints
a labeled RAW/FILT/THR/AMP/STATE line every sample for algorithm transparency and
report screenshot evidence.

Purpose: OUT-02 makes the device robust — removing the finger gives a clear "--"
signal and a fresh threshold, instead of stale BPM. OUT-04 gives the report a
serial capture showing a visible threshold-crossing event.
Output: A modified algorithm.c with FINGER_MIN_AMPLITUDE set from real hardware data.

DEPENDENCY: This plan depends on 04-01 because FINGER_MIN_AMPLITUDE cannot be chosen
without the with-finger / no-finger amplitude values measured by CALIBRATION_MODE in
Plan 1. Read 04-01-SUMMARY.md for those numbers before starting Task 1.
</objective>

<phase_goal>
**As a** user of the heart rate monitor, **I want to** see a clear "--" indication
when no finger is present and have the device re-acquire cleanly when I place my
finger back, **so that** the BPM reading is always trustworthy and never stale.
</phase_goal>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-integration-analog-calibration/04-RESEARCH.md
@.planning/phases/04-full-integration-analog-calibration/04-PATTERNS.md
@.planning/phases/04-full-integration-analog-calibration/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- algorithm.c current contracts — extracted from the codebase. -->

Existing file-scope statics in Src/algorithm.c (Phase 3, lines 9-26):
```c
static uint16_t  s_ma_buf[32];
static uint8_t   s_ma_idx;
static uint32_t  s_ma_sum;
#define REFRACTORY_MS  350U
typedef enum { IDLE, RISING, PEAK_HOLD, FALLING, REFRACTORY } peak_state_t;
static peak_state_t s_state;          /* init IDLE */
static uint16_t     s_threshold;      /* init 1200 */
static uint16_t     s_peak_val;       /* init 0 */
static uint32_t     s_refractory_start;
static uint32_t  s_last_peak_ms;
static uint16_t  s_bpm_buf[5];
static uint8_t   s_bpm_idx;
static uint8_t   s_bpm_count;
```

algorithm_process() locals already in scope (lines 58-69):
```c
void algorithm_process(uint16_t sample);   /* `sample` is the function parameter */
uint16_t filtered = (uint16_t)(s_ma_sum >> 5);   /* computed at line 66 */
peak_state_t next_state = s_state;               /* declared at line 69 */
```

From Inc/usart.h (the only serial output API):
```c
void uart_write_str(const char *s);
void uart_write_u32(uint32_t n);
```

From Inc/systick.h:
```c
uint32_t millis(void);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add FINGER_MIN_AMPLITUDE constant and the amplitude-tracking statics</name>
  <files>/home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c</files>
  <read_first>
    - /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c (the file being modified — see static declaration style at lines 9-27 and algorithm_init at lines 33-51)
    - .planning/phases/04-full-integration-analog-calibration/04-01-SUMMARY.md (READ FIRST — the with-finger / no-finger amplitude values measured on real hardware)
    - .planning/phases/04-full-integration-analog-calibration/04-PATTERNS.md (Patterns 2A and 2F — exact static declarations and algorithm_init additions)
    - .planning/phases/04-full-integration-analog-calibration/04-RESEARCH.md (Pattern 3, Pitfall 3 — choosing FINGER_MIN_AMPLITUDE)
  </read_first>
  <action>
    Insert into algorithm.c the no-finger-detection constants and statics, following
    PATTERNS.md Pattern 2A. Insert immediately after line 27 (after the
    `s_refractory_start` declaration, before the `/* ----------` separator comment):

    - `#define FINGER_MIN_AMPLITUDE` — set the value from 04-01-SUMMARY.md. Choose
      approximately 50% of the with-finger amplitude reported by CALIBRATION_MODE, and
      verify the chosen value sits clearly above the no-finger amplitude. If the
      SUMMARY data is missing or ambiguous, use `50U` as the conservative starting
      value documented in RESEARCH.md Assumption A1, and note in the SUMMARY that it
      still needs empirical confirmation. Add an inline comment stating the source
      value, e.g. "counts; ~50% of measured with-finger amp NNN (04-01-SUMMARY)".
    - `#define AMP_WINDOW_MS  1280U` — inline comment "1.28s window, ~1-2 heartbeat
      cycles at 60-80 BPM".
    - `static uint16_t  s_amp_min       = 4095;`
    - `static uint16_t  s_amp_max       = 0;`
    - `static uint32_t  s_amp_win_start = 0;`

    Then add three reset lines to `algorithm_init()` (PATTERNS.md Pattern 2F), inserted
    after `s_last_peak_ms = 0;` (line 50) and before the closing brace:
    `s_amp_min = 4095;`, `s_amp_max = 0;`, `s_amp_win_start = 0;` with a leading
    comment "Reset amplitude tracking window".

    Integer-only — FINGER_MIN_AMPLITUDE and AMP_WINDOW_MS are integer literals with a
    `U` suffix. No float anywhere (CLAUDE.md no-FPU constraint).
  </action>
  <verify>
    <automated>grep -q '#define FINGER_MIN_AMPLITUDE' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q '#define AMP_WINDOW_MS' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q 's_amp_win_start' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && ! grep -qE '\bfloat\b|\bdouble\b' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c</automated>
  </verify>
  <acceptance_criteria>
    - Source: algorithm.c contains `#define FINGER_MIN_AMPLITUDE` followed by an integer literal with a `U` suffix
    - Source: the FINGER_MIN_AMPLITUDE line has an inline comment referencing the 04-01-SUMMARY measured value (or, if data missing, noting it is the conservative 50U default)
    - Source: algorithm.c contains `#define AMP_WINDOW_MS  1280U`
    - Source: algorithm.c declares three new file-scope statics: `s_amp_min` (init 4095), `s_amp_max` (init 0), `s_amp_win_start` (init 0)
    - Source: algorithm_init() resets `s_amp_min = 4095;`, `s_amp_max = 0;`, `s_amp_win_start = 0;`
    - Source: algorithm.c contains NO `float` and NO `double` tokens anywhere
    - Behavior: project compiles cleanly in STM32CubeIDE with zero errors and zero new warnings
  </acceptance_criteria>
  <done>algorithm.c defines FINGER_MIN_AMPLITUDE (value sourced from real hardware measurement), AMP_WINDOW_MS, and the three amplitude-tracking statics; algorithm_init resets all three; compiles cleanly; no float.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement no-finger detection with threshold reset, and DEBUG_VERBOSE per-sample dump</name>
  <files>/home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c</files>
  <read_first>
    - /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c (current state after Task 1 — see algorithm_process lines 58-189)
    - .planning/phases/04-full-integration-analog-calibration/04-PATTERNS.md (Patterns 2B, 2C, 2D, 2E, 2G — exact insertion points and code; "Exact Insertion Map")
    - .planning/phases/04-full-integration-analog-calibration/04-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 2, Pitfall 5)
  </read_first>
  <action>
    Make two insertions into `algorithm_process()` in algorithm.c.

    INSERTION A — no-finger amplitude tracking + reset (PATTERNS.md Pattern 2G).
    Insert between line 66 (where `filtered` is computed) and line 69 (where
    `next_state` is declared). Open the block with a comment anchor:
    `/* AMPLITUDE_TRACKING_BEFORE_STATE_MACHINE */` — this token is used by the
    automated verify to confirm the insertion precedes the state machine switch.
    This runs on every sample, BEFORE the state machine, so the window sees raw
    `sample` values (larger swing than `filtered`).
    - On each call: `if (sample < s_amp_min) s_amp_min = sample;` and
      `if (sample > s_amp_max) s_amp_max = sample;`
    - Take `uint32_t now_amp = millis();`. When
      `(now_amp - s_amp_win_start) >= AMP_WINDOW_MS`: compute
      `uint16_t amplitude = (s_amp_max > s_amp_min) ? (uint16_t)(s_amp_max - s_amp_min) : 0;`,
      then reset the window: `s_amp_win_start = now_amp; s_amp_min = 4095; s_amp_max = 0;`.
    - Inside that window-expiry block, if `amplitude < FINGER_MIN_AMPLITUDE`:
      print `uart_write_str("--\r\n");` then reset state for clean re-acquisition
      (PATTERNS.md Pattern 2B no-finger partial reset):
      `s_threshold = 1200;`, `s_state = IDLE;`, `s_peak_val = 0;`,
      `s_bpm_count = 0;`, `s_bpm_idx = 0;`, then `return;` to skip the state machine
      this cycle. Do NOT zero the moving-average buffer s_ma_buf (RESEARCH.md Pattern 3
      — zeroing it causes a startup artifact). Resetting s_bpm_count and s_bpm_idx is
      mandatory (RESEARCH.md Pitfall 2 — otherwise stale BPM averages with new readings).

    INSERTION B — DEBUG_VERBOSE per-sample dump (PATTERNS.md Pattern 2E).
    Insert at the bottom of `algorithm_process()`, after the state-transition block
    (after the `}` that closes the `if (next_state != s_state)` block, which itself
    closes after line 188), before the function's final closing brace. Guard it with
    `#ifdef DEBUG_VERBOSE` / `#endif`. Inside, in a scoped `{ }` block:
    - Compute `uint16_t amp_display = (s_amp_max > s_amp_min) ? (uint16_t)(s_amp_max - s_amp_min) : 0;`
      (running window max/min as the AMP proxy).
    - Print, each on the same line, in this exact field order:
      `RAW:` then `sample`, ` FILT:` then `filtered`, ` THR:` then `s_threshold`,
      ` AMP:` then `amp_display`, ` ST:` then `(uint32_t)s_state`, then `\r\n`.
      Use uart_write_str for the labels and uart_write_u32 for every numeric value;
      cast the enum `s_state` to `(uint32_t)` for uart_write_u32 (state encoding
      0=IDLE,1=RISING,2=PEAK_HOLD,3=FALLING,4=REFRACTORY). Add a comment noting
      DEBUG_VERBOSE is ~35 chars/sample (~3ms TX at 8MHz/115200) — safely within the
      10ms sample interval (RESEARCH.md Pitfall 5) — and must be disabled for the
      production 30-second BPM accuracy capture.

    All arithmetic integer-only. uart_write_u32 takes uint32_t — cast where needed.
    No float, no printf, no stdlib (CLAUDE.md).
  </action>
  <verify>
    <automated>grep -q '"--\\r\\n"' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q '#ifdef DEBUG_VERBOSE' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q 'RAW:' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q 'amplitude < FINGER_MIN_AMPLITUDE' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && grep -q 'AMPLITUDE_TRACKING_BEFORE_STATE_MACHINE' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c && ! grep -qE '\bfloat\b|\bdouble\b|printf' /home/erkmen/STM32CubeIDE/workspace_2.1.1/HeartRateSensor1/Src/algorithm.c</automated>
  </verify>
  <acceptance_criteria>
    - Source: algorithm.c contains the no-finger check `if (amplitude < FINGER_MIN_AMPLITUDE)`
    - Source: the no-finger block prints the exact string `"--\r\n"` via uart_write_str
    - Source: the no-finger reset assigns `s_threshold = 1200;`, `s_state = IDLE;`, `s_peak_val = 0;`, `s_bpm_count = 0;`, and `s_bpm_idx = 0;`
    - Source: the no-finger reset block ends with `return;` and does NOT zero `s_ma_buf`
    - Source: amplitude tracking inserted before the state-machine `switch` so it sees raw `sample`, not `filtered`
    - Source: algorithm.c contains `#ifdef DEBUG_VERBOSE` and a matching `#endif`
    - Source: the DEBUG_VERBOSE block prints the labels `RAW:`, ` FILT:`, ` THR:`, ` AMP:`, ` ST:` and ends each line with `\r\n`
    - Source: the DEBUG_VERBOSE block casts `s_state` to `(uint32_t)` for uart_write_u32
    - Source: algorithm.c contains NO `float`, NO `double`, NO `printf` tokens
    - Behavior: project compiles cleanly with DEBUG_VERBOSE both defined and undefined (no unused-variable warnings either way)
  </acceptance_criteria>
  <done>algorithm.c prints "--\r\n" and resets threshold/state/BPM-average when the rolling amplitude window falls below FINGER_MIN_AMPLITUDE; with DEBUG_VERBOSE defined, every sample emits a labeled RAW/FILT/THR/AMP/ST line; integer-only; compiles cleanly with the define on or off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify no-finger detection, re-acquisition, and DEBUG_VERBOSE capture on hardware</name>
  <what-built>
    algorithm.c with OUT-02 no-finger detection (rolling 1.28s amplitude window,
    "--\r\n" output, threshold/state reset) and OUT-04 DEBUG_VERBOSE per-sample
    RAW/FILT/THR/AMP/ST dump.
  </what-built>
  <how-to-verify>
    PART A — no-finger detection and re-acquisition (DEBUG_VERBOSE OFF):
    1. Confirm `#define DEBUG_VERBOSE` is commented out in main.c and the
       `#define CALIBRATION_MODE` line is also commented out.
    2. Build in STM32CubeIDE and flash to the Nucleo-F070RB.
    3. Open a serial terminal at 115200 baud, 8N1.
    4. Place a finger on the sensor — confirm "BPM: NN" lines appear.
    5. Remove the finger — confirm "--" appears on the terminal within ~2 seconds
       (the 1.28s amplitude window plus one cycle; ROADMAP SC-3 requires within 2s).
    6. Place the finger back on the sensor — confirm BPM output resumes and the first
       readings are NOT contaminated by stale pre-removal values (re-acquisition
       succeeds because s_threshold/s_state/s_bpm_count were reset).
    7. If "--" prints intermittently WITH a finger steadily present, FINGER_MIN_AMPLITUDE
       is set too high — lower it in algorithm.c (RESEARCH.md Pitfall 3) and re-flash.
       If "--" never prints with no finger, the value is too low — raise it.

    PART B — DEBUG_VERBOSE capture for report evidence:
    8. In main.c, uncomment `#define DEBUG_VERBOSE`. Rebuild and flash.
    9. With a finger on the sensor, confirm the terminal streams per-sample lines in
       the format "RAW:NNN FILT:NNN THR:NNN AMP:NNN ST:N" at ~100 lines/second.
    10. Capture a serial log that contains at least one threshold-crossing event —
        a transition where FILT rises past THR and ST advances (e.g. ST:0 -> ST:1).
        Save this capture; it is required report evidence for OUT-04 (Phase 5 RPT-01).
    11. Confirm BPM readings do not drift high while DEBUG_VERBOSE is on (if they do,
        TX is starving the algorithm — RESEARCH.md Pitfall 5; should not happen at
        ~3ms TX per 10ms sample, but verify).
    12. Re-comment `#define DEBUG_VERBOSE` for the production / accuracy-capture build.

    Report back: whether "--" appeared within 2s of finger removal, whether
    re-acquisition was clean, the final FINGER_MIN_AMPLITUDE value used, and
    confirmation that a DEBUG_VERBOSE capture with a visible threshold crossing was saved.
  </how-to-verify>
  <resume-signal>Type "approved" with the final FINGER_MIN_AMPLITUDE value and confirmation the DEBUG_VERBOSE capture was saved, or describe any issue (intermittent "--", failed re-acquisition, BPM drift).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LM358 analog -> ADC sample stream | Uncontrolled real-world signal magnitude reaches algorithm_process() |
| algorithm_process() -> USART2 | Per-sample DEBUG_VERBOSE output competes with the 10ms sample interval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Denial of Service | DEBUG_VERBOSE per-sample TX blocking the main loop | mitigate | ~35 chars/sample = ~3ms TX at 8MHz/115200, well within the 10ms sample window; Task 3 Part B explicitly checks BPM does not drift high while DEBUG_VERBOSE is on |
| T-04-06 | Tampering | Stale rolling-average BPM after finger removal | mitigate | No-finger reset clears `s_bpm_count` and `s_bpm_idx`, forcing the rolling average to refill from scratch on re-acquisition (RESEARCH.md Pitfall 2) |
| T-04-07 | Denial of Service | s_state stuck in REFRACTORY/FALLING after finger removal blocks re-acquisition | mitigate | No-finger reset explicitly sets `s_state = IDLE` so the detector re-arms cleanly |
| T-04-08 | Tampering | Unsigned underflow in `s_amp_max - s_amp_min` | mitigate | Subtraction guarded by the ternary `(s_amp_max > s_amp_min) ? ... : 0` everywhere it is computed |
| T-04-09 | Spoofing | Weak real signal misread as no-finger (false "--") | accept | FINGER_MIN_AMPLITUDE tuned from CALIBRATION_MODE data and adjusted in Task 3; conservative low default avoids false positives |
</threat_model>

<verification>
- algorithm.c compiles cleanly with DEBUG_VERBOSE defined and undefined.
- grep confirms `"--\r\n"`, `#ifdef DEBUG_VERBOSE`, `RAW:`, and the no-finger check are present; no `float`/`double`/`printf`.
- On hardware: removing the finger prints "--" within 2 seconds; re-placing it resumes clean BPM output.
- On hardware: DEBUG_VERBOSE streams labeled RAW/FILT/THR/AMP/ST lines and a capture with a visible threshold crossing is saved.
</verification>

<success_criteria>
- OUT-02: no-finger detection prints "--\r\n" and resets the adaptive threshold to its initial value, enabling clean re-acquisition.
- OUT-04: DEBUG_VERBOSE prints labeled RAW/FILT/THR/AMP/STATE fields per sample; a capture shows at least one threshold-crossing event.
- FINGER_MIN_AMPLITUDE is locked to a value derived from real-hardware CALIBRATION_MODE measurement.
- All new code is integer-only; clock unchanged at 8MHz HSI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-full-integration-analog-calibration/04-02-SUMMARY.md`.
Record the final FINGER_MIN_AMPLITUDE value used and confirm the DEBUG_VERBOSE
threshold-crossing capture was saved for the Phase 5 report.
</output>
